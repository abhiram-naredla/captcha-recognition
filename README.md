# Captcha Recognition and Analysis 🧠🔤

This project uses machine learning and image processing to recognize and analyze CAPTCHA images.

## 📂 Project Structure
- `main.py` – Main program file  
- `requirements.txt` – Python dependencies  
- `Captcha_Recognition_Using_CNN/` – Model training code and scripts  
- `admins/`, `users/`, `templates/` – Django app folders  
- `db.sqlite3` – Local database file  
- `model.h5` – Trained model file  
- `staticzip.zip`, `mediazip.zip` – Contain static and media assets (extract before use)

## ⚙️ How to Run
1. Clone or download this repository.  
2. Extract the zipped folders (`staticzip.zip`, `mediazip.zip`).  
3. Install dependencies:
   ```bash
   pip install -r requirements.txt
